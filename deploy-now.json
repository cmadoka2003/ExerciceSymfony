{
    "type": "php",
    "buildCommand": "npm install && npm run build && composer install --no-dev --optimize-autoloader && php bin/console cache:clear",
    "startCommand": "php -S localhost:8000 -t public",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/public/index.php"
        }
    ]
}